import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-tests',
  imports: [RouterLink],
  templateUrl: './tests.html',
  styleUrl: './tests.scss',
})
export class Tests {}
